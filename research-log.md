# Research Log

| # | Date | Type | Summary |
|---|---|---|---|
| 1 | 2026-09-11 | bootstrap | Compared the ACPD method in `AAAI_2027_ACPD.pdf` with the historical V6.4 implementation. The paper jointly trains the selector and predictor and uses cue variance regularization; V6.4 stops selector gradients and omits that regularizer. Locked H1 for a paper-faithful LoRA/FSDP4 implementation. |
| 2 | 2026-09-11 | result | Backview smoke job 126759 completed two optimizer steps with global micro-batch 8, accumulation 4, effective batch 32, finite losses, nonzero selector/predictor/LoRA gradients, and 17,291 MiB sampled peak GPU memory per card. Physical global batch 32 smoke job 126936 also completed two steps with the same checks and 17,337 MiB sampled peak memory per card. Selected physical batch 32, accumulation 1 for the full runs. |
| 3 | 2026-09-12 | protocol | Cancelled the four pending 30K view-specific jobs before allocation. Locked H3: compare backview ACPD at layers 6, 12, and 6+12 for 5K steps using matched initialization, seed, data, and optimization. Primary metric is mean supervised loss over logged steps 100-4,900; the final-window metric covers steps 4,000-4,900. |
| 4 | 2026-09-12 | protocol | The scheduler estimated a one-week wait for four GPUs. Before H3 started, added a conditional infrastructure amendment: use FSDP2 with physical batch 32 for all three runs only if two-step job 127114 completes without OOM; otherwise retain FSDP4. |
| 5 | 2026-09-12 | execution | Job 127114 validated two-device FSDP at physical global batch 32 with about 31.4 GiB sampled peak memory per GPU. Staged the complete 123 GiB fixed dataset on debug01 local NVMe after NFS input stalls, then launched the controlled layer 6+12, layer 12, and layer 6 runs as jobs 127143, 127144, and 127145. The jobs run serially on the same node because each needs two of its three RTX 5090 GPUs. |
| 6 | 2026-09-12 | failure | The completed two-layer run wrote checkpoint 4999 successfully. The queued single-layer runs then encountered external GPU interference on debug01: another process occupied physical GPU0, causing layer 12 to abort during a 7.29 GiB XLA allocation and layer 6 to fail on a 143 MiB allocation. Their exact checkpoint directories were empty. The single-layer reruns will pin visible GPUs 1 and 2; no scientific setting changes. |
| 7 | 2026-09-12 | execution | The first pinned reruns still stalled in NFS checkpoint initialization, so the incomplete local checkpoint copies were abandoned per operator preference. The single-layer launchers now pin visible GPUs 1 and 2 while reading the original base and teacher paths; the fixed dataset remains local on debug01 NVMe. |
| 8 | 2026-09-12 | failure | Jobs 127245 and 127246 saw only one GPU because Slurm allocated two devices before the script narrowed visibility to physical GPUs 1 and 2. Both exited before model initialization with `Number of devices 1 must be divisible by ... FSDP devices 2`. The launchers now request all three debug01 GPUs and expose only GPUs 1 and 2 to JAX. |
| 9 | 2026-09-12 | correction | Cancelled debug01 layer-6 job 127363 before it produced any training step. Restored all 5K ablation launchers to the ordinary batch partition with two allocated GPUs, no physical GPU pinning, and the shared `/opt/liutong` dataset root. Future jobs wait for shared-storage reads; no datasets or checkpoints are copied to node-local storage. |
| 10 | 2026-09-12 | audit | Verified that Table 2 comes from the joint-selector V6 implementation, but the exact Table 1 ACPD scores come from V6.3 `historical_sg` runs. V6.3 stops gradients through the selected cue and omits the variance loss, so Table 1 is not evidence for the paper's stated Equations 2-5 implementation. |
| 11 | 2026-09-12 | result | Layers 12 and 6+12 have nearly identical 5K convergence: primary supervised loss is 0.04088 versus 0.04068 and final-window loss is 0.03257 versus 0.03237. The 0.61% final-window advantage is below the pre-registered 1% threshold. These runs test layer redundancy, not ACPD efficacy. |
| 12 | 2026-09-12 | protocol | Locked H4: compare flow-only, Cue-only, ACL-only, and the completed full-objective prefix for 2K steps inside the same distillation trainer. Added teacher task-loss diagnostics to test whether the frozen teacher is actually better on sampled flow targets. Diagnostic runs do not save final checkpoints. |
| 13 | 2026-09-12 | audit | Reconstructed Table 2 provenance. The reported Cue-only A result is ungated, Cue+ACL A is gated, and Cue+ACL A+W is ungated. Because the manuscript neither defines nor reports this gate, the rows do not form a controlled component or teacher-input ablation. |
| 14 | 2026-09-12 | execution | Submitted ordinary batch jobs 127402 (Flow-only), 127403 (Cue-only), and 127404 (ACL-only), each with FSDP2, physical global batch 32, 2K steps, and no checkpoint save. Existing layer-6 job 127379 remains untouched. |
| 15 | 2026-09-12 | audit | The old Table 1 and Table 2 launchers inherit `warmup_steps=10000`, `peak_lr=5e-5`, `decay_steps=1000000`, and `decay_lr=5e-5`. Despite the manuscript's cosine-decay description, equal peak and decay rates make the post-warmup schedule constant. The current LoRA schedule is therefore not a scaled copy of the reported setup. |
| 16 | 2026-09-13 | failure | Layer-6 job 127379 and ACL-only job 127404 failed on gpu03. NVML reported physical GPU4 as lost; the layer run exhausted an externally occupied allocation and ACL could not initialize CUDA. Excluded gpu03 from all ACPD Slurm launchers without changing dataset or checkpoint paths. |
| 17 | 2026-09-13 | execution | Resubmitted ACL-only as job 127437 and layer 6 as job 127438 in the ordinary batch queue. Slurm confirms both jobs exclude gpu03; all model, dataset, and checkpoint paths are unchanged. |
| 18 | 2026-09-13 | result | The pre-registered H4 component runs completed. Relative to Flow-only, Cue-only changed primary/final supervised loss by +0.80%/+0.98%, ACL-only by -0.31%/-0.63%, and Full ACPD by +0.11%/+0.16%. None reached the 1% decision threshold, so H4 is not supported at this resolution. The teacher was better on about 99.9% of sampled task-dimension flow targets. |
| 19 | 2026-09-13 | reflection | Both auxiliary branches alter their intended signals, but neither improves the early supervised-loss proxy materially. Deferred loss-weight tuning and selected a matched saved-checkpoint task-success comparison between Flow-only and Full ACPD as the next discriminating experiment. |
| 20 | 2026-09-13 | protocol | Locked H5: compare pooled four-suite backview success for matched Flow-only and Full ACPD step-4,999 checkpoints. A gain of at least 2 percentage points promotes component evaluation; smaller differences are inconclusive for this single-seed 5K screen. Only one new Flow-only training run is required because the Full ACPD checkpoint already exists. |
| 21 | 2026-09-13 | result | H3 completed. Layer 6, layer 12, and layers 6+12 achieved primary supervised losses of 0.040565, 0.040882, and 0.040682; final-window losses were 0.032250, 0.032570, and 0.032370. Layer 6 is best in both windows, so the two-layer hypothesis is not supported. |
| 22 | 2026-09-13 | execution | The matched H5 Flow-only job 127609 and H3 layer-6 job 127589 both completed and saved step-4,999 checkpoints. Full ACPD is only 0.21% and 0.40% lower than Flow-only in the primary and final loss windows; benchmark evaluation remains required. Removed disposable smoke checkpoints, failed-job logs, and local test caches while preserving successful experiment logs and all formal regression tests. |
| 23 | 2026-09-13 | record | Added `EXPERIMENT_RESULTS.md` as the concise Chinese result ledger and `AGENTS.md` as its mandatory read/update policy. Consolidated verified SFT benchmark results, ACPD ablations, and pending H5 evaluation without copying large artifacts into Git. |
| 24 | 2026-09-13 | protocol | Locked H6: run a 500-step, two-device visual-only probe. Freeze teacher and backview student, train only a linear probe to predict the teacher's layer-6 visual attention message, and compare correct versus batch-shuffled cosine. Require a final-window gap of at least 0.10 before implementing a new policy objective. |
| 25 | 2026-09-14 | protocol | Locked H6.1: compare the existing heuristic visual-message probe at action-expert layers 6, 9, and 12. Predict layer 9 will have the largest correct-minus-shuffled cosine gap; require a gap of 0.10 to pass and a 0.02 margin to select one layer. New jobs use no DataLoader subprocesses because the layer-6 job stalled on shared-storage worker reads. |
| 26 | 2026-09-14 | reflection | Audited H6 before it produced a measurement. Its normalized hidden-to-visual logits are divided by `sqrt(1024)`, making attention nearly uniform, and its whole-target shuffle does not isolate visual pairing. Cancelled stalled job 127810 after 4h26m with no first batch. H6 and H6.1 are abandoned without a result. |
| 27 | 2026-09-14 | protocol | Locked H7: in one two-GPU job, compare layers 6, 9, and 12 using the teacher's exact gated per-view attention residual, an episode-held-out split, a same-query visual-K/V shuffle, three probe seeds, and a teacher-advantage hard subset. Require positive held-out explained variance and pre-registered cosine-gap confidence bounds before policy training. |
| 28 | 2026-09-14 | literature | Reviewed privileged visual-policy distillation, Learning by Cheating, RMA, motion-planner policy distillation, and Theia. Across these methods, useful privilege is either recoverable from student observations/history or delivered as direct policy supervision on relevant states. This supports H7's recoverability gate and a temporal-history pivot for occluded backview samples. |
| 29 | 2026-09-14 | protocol | Strengthened H5 before evaluation: compare the two policies on identical task initial states and require both a pooled gain of at least 2 percentage points and a positive paired episode-bootstrap 95% confidence interval. Added matched four-GPU launchers whose only experimental difference is the step-4,999 checkpoint path. |
| 30 | 2026-09-14 | failure | H7 job 128036 restored both frozen models but failed on its first sampled frame. LeRobot retained global episode IDs after loading a noncontiguous episode list while indexing a compact 1,800-episode boundary table, causing `IndexError: index 1887 is out of bounds`. The scientific split is unchanged; the loader now loads the complete dataset and selects global frame ranges with `torch.utils.data.Subset`, preserving valid action-horizon queries and reusing the complete-dataset cache. |
| 31 | 2026-09-14 | failure | H5 Full ACPD evaluation job 128096 started all four policy servers, but none became healthy before the launcher's fixed 30-minute timeout. The four suite logs were empty because the final silent health check exited without a diagnostic. Extended only the operational startup wait to 90 minutes and added an explicit timeout error; checkpoints, seeds, trials, camera, and paired-analysis protocol remain unchanged. |
| 32 | 2026-09-14 | failure | H5 Flow-only evaluation job 128095 completed all 2,000 episodes (`spatial=500`, `object=500`, `goal=500`, `libero_10=500`) but returned nonzero after its shared launcher was edited while Bash was blocked inside the final evaluator. Bash resumed at a stale file offset and interpreted `-steps-wait` as a command. The four logs are complete and parse to `9/38/42/0` successes, so they remain valid; future launcher edits must wait until all jobs using that file have exited. |
| 33 | 2026-09-14 | reflection | Retired the four unused view-specific 30K ACPD launchers because H3/H4 do not justify long runs of the original objective. Removed only the empty H4 checkpoint directories; preserved all protocols, logs, tests, valid checkpoints, and negative results. Flow-only H5 evaluation is complete at 89/2,000 successes (4.45% pooled), while Full ACPD and H7 remain active. |
| 34 | 2026-09-14 | result | H5 passed its pre-registered screen. Full ACPD reached 130/2,000 successes (6.50%) versus Flow-only at 89/2,000 (4.45%), a +2.05-point gain with task-stratified paired-bootstrap 95% CI [+0.90, +3.25] points. This single-seed 5K result supports further component testing but does not identify whether Cue or ACL caused the gain. |
| 35 | 2026-09-14 | protocol | Locked H8: train the matched ACL-only policy for 5K steps and evaluate the same 2,000 episodes. If Full ACPD exceeds ACL-only by at least 1.5 points with a positive paired 95% CI, the old Cue has evidence beyond ACL; if ACL-only is within 0.5 points of Full, H5 is explained by ACL at this resolution. No repeated seed or view expansion is included. |
| 37 | 2026-09-14 | execution | Submitted H8 ACL-only 5K training as job 128417 on two-device FSDP with physical global batch 32. Submitted its matched four-suite evaluation as dependent job 128421. H7 job 128248 remained healthy and reached step 400/500. |
| 38 | 2026-09-14 | result | H7 job 128248 completed. Layer 9 passed all recoverability gates with overall gap 0.3417 (95% CI [0.3095, 0.3756]), hard-subset gap 0.3194 ([0.2717, 0.3875]), and explained variance 0.2822. Layer 12 also passed at gap 0.2367; layer 9 exceeded it by 0.1050. Layer 6 failed due to negative explained variance. |
| 40 | 2026-09-15 | execution | H8 ACL-only job 128417 completed all 5K steps without NaN, Inf, or traceback and finalized checkpoint 4999 with both params and assets. Step 4,900 supervised loss was 0.0316 and action-correlation loss was 0.0472; these are training-health diagnostics, not task-success evidence. Evaluation job 128421 became eligible. |
| 43 | 2026-09-15 | cleanup | Removed only artifact-free failed-run remnants, generated Python caches, and pytest caches. Preserved all scientific checkpoints, completed validation videos and logs, negative-result protocols, formal regression tests, and successful H7 evidence. |
| 45 | 2026-09-15 | protocol | Locked H7.1 to complete the local action-expert layer scan. Reuse the H7 teacher, student, episode split, shuffled-view control, three probe seeds, 500-step budget, and decision thresholds; test only the missing layers 7, 8, 10, and 11, then combine them with layers 6, 9, and 12. This is a recoverability test, not task-success evidence. |
| 46 | 2026-09-15 | execution | Submitted H7.1 layer-scan job 128789 to the normal batch partition. It requests two GPUs, 16 CPUs, and 48 GB host memory, preserving the four-GPU training ratio of 24 GB per GPU. The scientific configuration remains the locked H7 protocol. |
| 47 | 2026-09-16 | result | H8 evaluation completed all 2,000 episodes. ACL-only reached 6.35% pooled success versus Flow-only at 4.45% and Full ACPD at 6.50%. ACL-only minus Flow-only is +1.90 points with paired 95% CI [+0.75, +3.10]; Full minus ACL-only is +0.15 points with CI [-1.15, +1.40]. Under the locked 0.5-point rule, ACL explains the H5 gain and the old Cue has no detected incremental pooled benefit. |
| 48 | 2026-09-16 | result | H7.1 job 128789 completed the fixed layer scan. Layer 10 has overall gap 0.3992, hard gap 0.3779, and explained variance 0.3556. It exceeds runner-up layer 11 by 0.0485, above the locked 0.02 margin, so the combined layer-6-through-12 scan rejects layer 9 as the stable local optimum and selects layer 10. This is recoverability evidence, not task-success evidence. |
| 51 | 2026-09-16 | implementation | Added the layer-10 deployment config, 5K training launcher, and four-suite evaluation launcher. All 12 distillation tests, Ruff, and Bash syntax checks pass. |
| 53 | 2026-09-17 | protocol | H9 tests only the layer-10 exact-contribution target selected by H7.1. It uses four-device FSDP, physical global batch 32, no gradient accumulation, and the unchanged 5K decision rule against H8 ACL-only. |
| 54 | 2026-09-17 | execution | Stopped the superseded micro-batch-8 jobs before they produced a checkpoint or evaluation result. H9 training job 129710 passed four finite optimizer steps with nonzero LoRA, predictor, and gate gradients at 17,402 MiB/card; dependent four-suite evaluation job 129711 waits for checkpoint 4,999. |
| 55 | 2026-09-17 | protocol | Extended H9 with a locked infrastructure comparison: H9-scale-a uses FSDP2, physical global BS32, local BS16, and 30K steps; H9-scale-b uses FSDP4, physical global BS64, local BS16, and 30K steps. Both use accumulation 1. The sample budgets differ, so this measures runtime and memory rather than controlled policy quality. |
| 56 | 2026-09-17 | execution | Submitted H9-scale-a as job 129727 and H9-scale-b as job 129728. Both started immediately on ordinary batch nodes and passed their first finite optimizer step without OOM. H9-scale-b reached step 100 at about 6.0 seconds per step and 31,464 MiB/card; the four-GPU BS32 reference was 8.1--8.6 seconds per step before concurrent load. |
| 57 | 2026-09-17 | execution | Cancelled H9-scale-a job 129727 by operator decision at step 83. It was healthy at 12.0--12.6 seconds per step with 31,388 MiB/card, but was about 1.45 times slower per optimizer step than four-device BS32. It did not reach the first checkpoint save and provides no task-success result. Jobs 129710, 129711, and 129728 remain unchanged. |
| 58 | 2026-09-17 | protocol | Locked H11: compare layer-10 attention-to-FFN injection against H9-scale-b final-hidden injection with the same FSDP4, physical BS64, seed, schedule, 30K budget, and step-4,999 four-suite evaluation. Require at least +1.5 pooled points and a positive paired-bootstrap 95% CI lower bound. H10 loss-weight calibration remains deferred until the injection structure is selected. |
| 59 | 2026-09-17 | execution | Implemented single-forward H11 fusion with zero-gate parity and stop-gradient predictor injection. All 16 targeted tests and static checks pass. Submitted four-device BS64 smoke job 129807; formal 30K job 129808 has an afterok dependency on the smoke. Both are pending while the user's eight GPUs run H9 jobs. |
| 60 | 2026-09-18 | result | H9 completed its pre-registered 5K screen. Layer-10 ACPD-v2 reached 227/2,000 successes (11.35%) versus H8 ACL-only at 127/2,000 (6.35%). The +5.00-point paired difference has task-stratified bootstrap 95% CI [+3.60, +6.45], so H9 passes both decision gates. |
| 61 | 2026-09-18 | execution | H9-scale-b checkpoint 4,999 completed four-suite evaluation at 463/2,000 successes (23.15%) and is the locked BS64 final-hidden control for H11; it is not a controlled batch-size comparison with H9. Job 129728 continues near step 8,280. H11 smoke job 129807 passed and formal job 129808 is healthy near step 1,320 on four GPUs. |
| 62 | 2026-09-18 | protocol | Locked H12 and submitted job 130285: standard backview-only pi0.5 LoRA with FSDP4, physical global BS64, seed 42, and the H9-scale-b cosine schedule for 5K steps. Checkpoint 4,999 will be evaluated on the same 2,000 episodes and compared with H9-scale-b using the pre-registered +1.5-point and positive paired-CI gates. The job is pending for resources. |
| 63 | 2026-09-19 | execution | H9-scale-b job 129728 reached about step 18,800, H11 job 129808 about step 8,090, and matched SFT baseline H12 job 130285 about step 3,580. All three remain finite with no OOM or traceback. H11 checkpoint 4,999 is available but has not been evaluated; H9 retains checkpoint 14,999 while its 4,999 evaluation result remains recorded separately. |
| 64 | 2026-09-19 | analysis | Audited H9 training and validation together. The 5K physical-BS32 run completed 5,000 optimizer steps with 17,406 MiB/card peak memory, supervised loss 0.0892→0.0311, exact-contribution cosine 0.0057→0.6980, and no NaN/OOM/traceback. H9 reached 11.35% pooled success versus H8 ACL-only 6.35% (paired 95% CI [+3.60, +6.45]). This supports the ACPD-v2 design as an effective single-seed 5K screen, but the final gate is only 0.0048 and predictor injection is stop-gradient, so direct residual injection is not isolated from auxiliary representation regularization; H11, matched SFT, and repeated seeds remain necessary. |
| 65 | 2026-09-19 | execution | Slurm check at 11:17 CST found three running jobs and no queued jobs: H9-scale-b job 129728 on gpu01 near step 19,300, H11 job 129808 on gpu05 near step 8,300, and H12 matched SFT job 130285 on gpu01 near step 4,800. Each allocates 4 GPUs, 32 CPUs, and 96G memory; all latest logs remain finite. |
| 66 | 2026-09-19 | execution | H11 checkpoint 4999 was verified at the exact injection-location path and evaluation job 130490 was submitted with the four-suite aligned-attention launcher. H12 training job 130285 completed step 4999 and finalized its checkpoint; step 4900 loss was 0.0290. A new BS64 SFT evaluation launcher was syntax-checked and submitted as job 130491. Both evaluations are pending Slurm resources/priority. |
| 67 | 2026-09-19 | execution | At 13:25 CST, H11 evaluation job 130490 was running on gpu01 and all four suites had entered episode execution without traceback, CUDA, OOM, or startup errors; no final success rates were available. H12 evaluation job 130491 remained pending resources. H9-scale-b training reached about step 20,300 and H11 training reached about step 9,000. |
| 68 | 2026-09-19 | execution | Stopped H11 training job 129808 by operator decision at logged step 9,500 because checkpoint 4,999 already isolates the injection-location question; only checkpoint 4,999 is retained and its evaluation job 130490 continues. Releasing gpu05 allowed matched-SFT evaluation job 130491 to start. H9-scale-b job 129728 continues near step 20,900. |
| 69 | 2026-09-19 | protocol | Locked H13: compare H9-scale-b final-hidden injection with a matched loss-only exact-contribution run. Both use layer 10, FSDP4, physical BS64, seed 42, identical losses and schedule, and checkpoint 4,999; H13 only disables contribution injection in both training and inference. Primary evidence is the paired 2,000-episode success difference with a 1.5-point equivalence margin. |
| 70 | 2026-09-19 | execution | Added an independent injection switch while preserving the exact-contribution predictor and loss. Twelve targeted tests, Ruff, CLI, and Bash checks passed. At operator direction, cancelled the pending four-device smoke chain and submitted H13 5K training as job 130599 directly; evaluation job 130600 depends on successful training. Both use FSDP4 and physical BS64. |
| 71 | 2026-09-19 | result | H11 completed 2,000 paired episodes at checkpoint 4,999. Layer-10 same-layer injection reached 21.20% pooled success versus H9-scale-b final-hidden injection at 23.15%; H11-H9 is -1.95 points with task-stratified paired-bootstrap 95% CI [-4.10, +0.20]. H11 fails its pre-registered improvement gates, so final-hidden injection remains the selected structure. |
